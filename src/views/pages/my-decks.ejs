<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meus Decks - One Piece TCG</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/deck-builder.css"> <!-- Reusing styles -->
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="deck-builder-container">
        <h1>Meus Decks</h1>
        <a href="/deck-builder" class="btn-primary">Criar Novo Deck</a>
        <div class="decks-list">
            <% if (decks && decks.length > 0) { %>
                <% decks.forEach(deck => { %>
                    <div class="deck-item">
                        <h2><%= deck.title %></h2>
                        <p><%= deck.description %></p>
                        <a href="/deck-builder/<%= deck._id %>">Editar</a>
                    </div>
                <% }); %>
            <% } else { %>
                <p>Você ainda não tem nenhum deck. <a href="/deck-builder">Crie um agora!</a></p>
            <% } %>
        </div>
    </div>

    <%- include('../partials/footer') %>

    <script src="/js/main.js"></script>
</body>
</html>