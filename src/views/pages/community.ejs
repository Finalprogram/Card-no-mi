<%- include('../partials/header') %>
<%- include('../partials/navbar') %>

<style>
  .community-page {
    max-width: 1180px;
    margin: 0 auto;
    padding: 2rem 1rem 3rem;
  }

  .community-hero {
    background: linear-gradient(145deg, rgba(124, 108, 255, 0.16), rgba(255, 185, 66, 0.12));
    border: 1px solid rgba(124, 108, 255, 0.2);
    border-radius: 18px;
    padding: 1.8rem 1.6rem;
    margin-bottom: 1.4rem;
  }

  .community-hero h1 {
    margin: 0 0 0.5rem;
    color: #fff;
    font-size: clamp(1.7rem, 2.3vw, 2.2rem);
    font-weight: 800;
  }

  .community-hero p {
    margin: 0;
    color: #cfd2dc;
    font-size: 1rem;
    line-height: 1.55;
    max-width: 780px;
  }

  .community-section {
    margin-top: 1.25rem;
    background: #12151f;
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 16px;
    padding: 1.25rem;
  }

  .community-section h2 {
    margin: 0 0 0.35rem;
    color: #fff;
    font-size: 1.15rem;
    font-weight: 800;
  }

  .community-section .section-desc {
    margin: 0 0 1rem;
    color: #a9afbf;
    font-size: 0.95rem;
  }

  .creators-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(210px, 1fr));
    gap: 0.85rem;
  }

  .creator-card {
    display: block;
    text-decoration: none;
    border-radius: 14px;
    padding: 0.9rem;
    background: #181c27;
    border: 1px solid rgba(255, 255, 255, 0.08);
    color: #fff;
    transition: transform 180ms ease, border-color 180ms ease, background-color 180ms ease;
  }

  .creator-card:hover {
    transform: translateY(-2px);
    border-color: rgba(124, 108, 255, 0.45);
    background: #1d2230;
  }

  .creator-top {
    display: flex;
    align-items: center;
    gap: 0.7rem;
  }

  .creator-logo {
    width: 52px;
    height: 52px;
    border-radius: 50%;
    border: 2px solid rgba(124, 108, 255, 0.45);
    object-fit: cover;
    background: #0f1115;
  }

  .creator-name {
    margin: 0;
    font-weight: 700;
    font-size: 0.98rem;
    line-height: 1.35;
  }

  .creator-platform {
    margin-top: 0.1rem;
    font-size: 0.8rem;
    color: #adb5c9;
  }

  .creator-cta {
    margin-top: 0.7rem;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.8rem;
    color: #f5b942;
    font-weight: 700;
  }

  .community-links-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 0.85rem;
  }

  .partner-stores-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 0.85rem;
  }

  .partner-store-card {
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
    border-radius: 14px;
    padding: 0.95rem;
    background: #181c27;
    border: 1px solid rgba(255, 255, 255, 0.08);
    color: #fff;
    text-decoration: none;
    transition: transform 180ms ease, border-color 180ms ease, background-color 180ms ease;
  }

  .partner-store-card:hover {
    transform: translateY(-2px);
    border-color: rgba(245, 185, 66, 0.55);
    background: #1e2331;
  }

  .partner-store-top {
    display: flex;
    align-items: center;
    gap: 0.7rem;
  }

  .partner-store-logo {
    width: 52px;
    height: 52px;
    border-radius: 12px;
    background: #0f1115;
    object-fit: cover;
    border: 1px solid rgba(255, 255, 255, 0.08);
  }

  .partner-store-name {
    margin: 0;
    font-weight: 700;
    font-size: 0.98rem;
  }

  .partner-store-meta {
    margin-top: 0.1rem;
    font-size: 0.8rem;
    color: #adb5c9;
  }

  .partner-store-links {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .partner-store-links a {
    font-size: 0.75rem;
    color: #f5b942;
    font-weight: 700;
    text-decoration: none;
  }

  .partner-store-links a:hover {
    text-decoration: underline;
  }

  .partner-store-form {
    margin-top: 1rem;
    padding: 1rem;
    border-radius: 14px;
    background: #171b26;
    border: 1px dashed rgba(255, 255, 255, 0.08);
  }

  .partner-store-form h3 {
    margin: 0 0 0.6rem;
    color: #fff;
    font-size: 1rem;
  }

  .partner-store-form .form-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 0.75rem;
  }

  .partner-store-form input,
  .partner-store-form textarea {
    width: 100%;
    padding: 0.6rem 0.7rem;
    border-radius: 10px;
    border: 1px solid rgba(255, 255, 255, 0.08);
    background: #0f1115;
    color: #e6e8ee;
    font-size: 0.9rem;
  }

  .partner-store-form textarea {
    min-height: 90px;
    resize: vertical;
  }

  .partner-store-form button {
    margin-top: 0.75rem;
    padding: 0.65rem 1.2rem;
    border: none;
    border-radius: 10px;
    font-weight: 700;
    background: #f5b942;
    color: #1b1f2b;
    cursor: pointer;
    transition: transform 160ms ease, filter 160ms ease;
  }

  .partner-store-form button:hover {
    transform: translateY(-1px);
    filter: brightness(1.05);
  }

  .community-link-card {
    display: block;
    text-decoration: none;
    border-radius: 14px;
    padding: 1rem;
    background: #171b26;
    border: 1px solid rgba(255, 255, 255, 0.08);
    color: #fff;
    transition: transform 180ms ease, border-color 180ms ease, background-color 180ms ease;
  }

  .community-link-card:hover {
    transform: translateY(-2px);
    background: #1d2230;
  }

  .community-link-card h3 {
    margin: 0 0 0.45rem;
    font-size: 1.02rem;
    font-weight: 700;
  }

  .community-link-card p {
    margin: 0;
    color: #b3bac8;
    font-size: 0.9rem;
    line-height: 1.5;
  }

  .community-link-card.discord:hover {
    border-color: rgba(114, 137, 218, 0.55);
  }

  .community-link-card.whatsapp:hover {
    border-color: rgba(37, 211, 102, 0.55);
  }
</style>

<main class="community-page">
  <section class="community-hero">
    <h1>Comunidade Card no Mi</h1>
    <p>Conteudo, estrategia e trocas em um so lugar. Acompanhe criadores parceiros, entre nas comunidades e fique por dentro de tudo do One Piece TCG.</p>
  </section>

  <section class="community-section">
    <h2>Criadores Parceiros</h2>
    <p class="section-desc">Destaque para quem cria conteudo da comunidade. Clique no canal para abrir as redes.</p>
    <div class="creators-grid">
      <a href="https://www.youtube.com/@matugabrazil" target="_blank" rel="noopener noreferrer" class="creator-card">
        <div class="creator-top">
          <img src="/images/logo_YT/MatugaBrazil.jpg" alt="Logo do canal parceiro 1" class="creator-logo">
          <div>
            <p class="creator-name">Matuga Brazil</p>
            <div class="creator-platform">YouTube</div>
          </div>
        </div>
        <span class="creator-cta">Ver canal</span>
      </a>

      <a href="https://www.youtube.com/@farofadobardo" target="_blank" rel="noopener noreferrer" class="creator-card">
        <div class="creator-top">
          <img src="/images/logo_YT/FarofaBardo.jpg" alt="Logo do canal parceiro 2" class="creator-logo">
          <div>
            <p class="creator-name">Farofa do Bardo</p>
            <div class="creator-platform">YouTube</div>
          </div>
        </div>
        <span class="creator-cta">Ver canal</span>
      </a>

      <a href="https://www.youtube.com/bernalkaiba1" target="_blank" rel="noopener noreferrer" class="creator-card">
        <div class="creator-top">
          <img src="/images/logo_YT/Bernalkaiba.jpg" alt="Logo do canal parceiro 3" class="creator-logo">
          <div>
            <p class="creator-name">Bernalkaiba</p>
            <div class="creator-platform">YouTube / X</div>
          </div>
        </div>
        <span class="creator-cta">Ver canal</span>
      </a>

      <!-- <a href="https://www.youtube.com/" target="_blank" rel="noopener noreferrer" class="creator-card">
        <div class="creator-top">
          <img src="/images/default-avatar.png" alt="Logo do canal parceiro 4" class="creator-logo">
          <div>
            <p class="creator-name">Canal Parceiro 04</p>
            <div class="creator-platform">YouTube / Twitch</div>
          </div>
        </div>
        <span class="creator-cta">Ver canal</span>
      </a> -->
    </div>
  </section>

  <section class="community-section">
    <h2>Lojas Parceiras</h2>
    <p class="section-desc">Lojas oficiais e parceiras que apoiam a comunidade. Clique para conhecer.</p>
    <div class="partner-stores-grid">
      <% if (partnerStores && partnerStores.length) { %>
        <% partnerStores.forEach((store) => { %>
          <div class="partner-store-card">
            <div class="partner-store-top">
              <img src="<%= store.logoUrl || '/images/default-avatar.png' %>" alt="Logo da loja <%= store.storeName %>" class="partner-store-logo">
              <div>
                <p class="partner-store-name"><%= store.storeName %></p>
                <div class="partner-store-meta"><%= [store.city, store.state].filter(Boolean).join(' • ') || 'Loja Parceira' %></div>
              </div>
            </div>
            <div class="partner-store-links">
              <% if (store.websiteUrl) { %>
                <a href="<%= store.websiteUrl %>" target="_blank" rel="noopener noreferrer">Site</a>
              <% } %>
              <% if (store.instagramUrl) { %>
                <a href="<%= store.instagramUrl %>" target="_blank" rel="noopener noreferrer">Instagram</a>
              <% } %>
              <% if (store.whatsappUrl) { %>
                <a href="<%= store.whatsappUrl %>" target="_blank" rel="noopener noreferrer">WhatsApp</a>
              <% } %>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p class="section-desc">Em breve novas lojas parceiras por aqui.</p>
      <% } %>
    </div>

  </section>

  <section class="community-section">

    <h2>Conexoes da Comunidade</h2>
    <p class="section-desc">Grupos oficiais para conversar, tirar duvidas e acompanhar anuncios.</p>
    <div class="community-links-grid">
      <a href="https://discord.gg/s4pdaqq8Ur" class="community-link-card discord" target="_blank" rel="noopener noreferrer">
        <h3>Discord</h3>
        <p>Servidor para discussao, organizacao de partidas, trocas e canais por assunto.</p>
      </a>

      <a href="https://chat.whatsapp.com/FPctK5BXTuw3JrCfOb87Az" class="community-link-card whatsapp" target="_blank" rel="noopener noreferrer">
        <h3>WhatsApp</h3>
        <p>Grupo para avisos rapidos, novidades da plataforma e comunicacao direta.</p>
      </a>
    </div>
  </section>
</main>

<%- include('../partials/footer') %>
