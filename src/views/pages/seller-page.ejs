<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página do Vendedor - <%= seller.username %></title>
    <!-- Você pode querer adicionar um CSS específico para esta página -->
    <!-- <link rel="stylesheet" href="/css/seller-page.css"> -->
    <style>
        /* Estilos básicos para demonstração */
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f4; }
        .container { max-width: 1200px; margin: 20px auto; padding: 20px; }
        .seller-header { border-bottom: 2px solid #ddd; padding-bottom: 20px; margin-bottom: 20px; }
        .seller-header h1 { margin: 0; }
        .listings-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 20px; }
        .listing-card { background-color: #fff; border: 1px solid #ddd; border-radius: 8px; padding: 15px; text-align: center; }
        .listing-card img { max-width: 100%; height: auto; border-radius: 4px; }
        .listing-card h3 { font-size: 1em; margin: 10px 0 5px; }
        .listing-card p { margin: 5px 0; color: #555; }
        .no-listings { text-align: center; padding: 50px; background-color: #fff; border-radius: 8px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="seller-header">
            <h1>Anúncios de <%= seller.username %></h1>
            <!-- Futuramente, pode adicionar mais informações do vendedor aqui, como reputação, etc. -->
        </div>

        <% if (listings && listings.length > 0) { %>
            <div class="listings-grid">
                <% listings.forEach(listing => { %>
                    <div class="listing-card">
                        <% if (listing.card && listing.card.image_url) { %>
                            <a href="/card/<%= listing.card.slug %>"> <!-- Link para a página da carta -->
                                <img src="<%= listing.card.image_url %>" alt="<%= listing.card.name %>">
                            </a>
                        <% } %>
                        <h3><%= listing.card ? listing.card.name : 'Carta não encontrada' %></h3>
                        <p><strong>Preço:</strong> R$ <%= listing.price.toFixed(2) %></p>
                        <p><strong>Condição:</strong> <%= listing.condition %></p>
                        <p><strong>Idioma:</strong> <%= listing.language %></p>
                        <p><strong>Disponível:</strong> <%= listing.quantity %></p>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="no-listings">
                <h2>Este vendedor ainda não tem nenhum anúncio ativo.</h2>
            </div>
        <% } %>
    </div>
</body>
</html>
